h1 Artwork
br
a(href="#" ng-click="toggle=true" ng-show="$parent.loggedInUser._id === $parent.project._user._id && !toggle").btn.btn-primary Add artwork

form(ng-show="toggle").bio-edit
	.form-group
		input(type="file")
	button(type="submit").btn.btn-primary.btn-inline Submit
	button(ng-click="toggle=false").btn.btn-primary.btn-inline Cancel
